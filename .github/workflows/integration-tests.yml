name: Integration Tests

on:
  pull_request:
    branches: [ main ]

jobs:
  integration-tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.21'

    - name: Create k3d cluster
      uses: AbsaOSS/k3d-action@v2
      with:
        cluster-name: "test-cluster"
        args: >-
          --agents 1
          --no-lb
          --k3s-arg "--disable=traefik,servicelb,metrics-server@server:*"

    - name: Install go-ctrf-json-reporter
      run: go install github.com/ctrf-io/go-ctrf-json-reporter/cmd/go-ctrf-json-reporter@latest

    - name: Run integration tests with coverage and generate CTRF report
      run: |
        go test -v ./... -tags=integration -json -coverprofile=coverage.txt -covermode=atomic | go-ctrf-json-reporter -output ctrf-report.json
      
    - name: Display Coverage
      if: always()
      run: |
        go tool cover -func=coverage.txt

    - name: Archive code coverage results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: code-coverage
        path: coverage.txt

    - name: Archive test results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: ctrf-report
        path: ctrf-report.json

    - name: Publish Test Results
      if: always()
      run: |
        npx github-actions-ctrf ctrf-report.json

  coverage-report:
    name: "Code coverage report"
    runs-on: ubuntu-latest
    needs: integration-tests
    permissions:
      contents: read
      actions: read
      pull-requests: write
    steps:
      - uses: fgrosse/go-coverage-report@v1.2.0
        with:
          coverage-artifact-name: "code-coverage"
          coverage-file-name: "coverage.txt"
          trim: "k8stool/" 